# Copie este arquivo como `.env` na raiz do repositório (gitignored) e preencha.
# Nunca commitar valores reais.

# === Aplicação ==========================================================
APP_HOST=0.0.0.0
APP_PORT=8000
